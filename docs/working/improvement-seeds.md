# Improvement Seeds（append-only / WF-06 Retro）

> 正本スキーマ: [`docs/ai/retro-phase.md`](../ai/retro-phase.md) §2。
> 1 run 1 エントリを追記のみ（既存編集・削除しない）。確定追記は人間 confirm でのみ。
> スコアリング・優劣判定は含めない（#231 LLM-judge の責務）。

## 2026-06-07 — v8.12.0 リリース run（#470-482 / 6 issue close）

- 目的達成可否: 達成。plugin sync 品質改善（#456/#476）・HO 待ち 4 件適用（#451/#452/#454/#463）・why-plangate ランディング（#470）を完遂し v8.12.0 を本番リリース（tag-main parity OK / Release published）。オープン PR・issue ともにゼロ。
- 失敗・手戻り: 44 コミット中 24 がレビュー指摘対応（55%）、fix が最多 16。シェルスクリプトの定石（set -eu 耐性・trap 隔離・パーミッション保持・tmp 掃除）を初回で外しレビューで修正するループが多発。特に trap は「なし→追加(#475)→Gemini high で親シェル汚染指摘→サブシェル隔離」と修正が連鎖。version 多箇所（marketplace metadata.version / README 散文 Latest 表記）の同期漏れも初回で発生。
- 次回再利用すべき判断: ① シェル一時リソースの掃除方式（サブシェル隔離 / trap なし通常 rm）を書く前に決める ② コマンド置換は if 条件内（set -e 耐性）③ 実ファイル改変は cat > で上書き（パーミッション保持）④ version 同期マップ（plugin.json / marketplace plugins[]+metadata / README Version 行+散文 Latest / CHANGELOG / tag）を 1 チェックリスト化し、sync 非対応の散文は drift テストで担保（ta-28 TC-11 で実装済）。
- 効いた skill / gate / artifact: 3 視点レビュー（セルフ機械 + 別視点 general-purpose + Gemini）が初回品質の低さを出口で全件救済。verify-then-report（CI ログで set -eu 停止・slug 不一致の原因特定、推測修正を回避）。責務 4 分類（apply-ho-followups.sh = AI 作成・Human 実行）で HO パスを正しい分界で適用。tag-main parity Iron Law。
- 1 人運用で負荷が高かった箇所: gh account ドリフト（毎 push 前に gh auth switch + https remote が必須）。Codex usage limit（6/11 まで）で相談が Codex 実行でなく直接判断に。レビュー往復が多く 1 機能あたりコミット数が増加。
- confirmed_by: masatake.komine（「記録して」指示にて confirm）

## 2026-06-25 — TASK-0143 EH-4/5/7 CLI 配線 run（#528 EH-3 doc-light 経路 / EPIC #527 子 PBI-1）

- 目的達成可否: 達成。EH-4 strict を `cmd_verify` V-1 前、EH-5 warn を V-1 後に配線する apply-script・dry-run 差分確認・ta-44 テスト（SKIP/PASS 両モード）・docs 2 件（settings-wiring-contract / hook-enforcement）を完遂。metrics 2 events 収集。HO 待ち（H-02 apply）は Human-owned として残す。
- 失敗・手戻り: Write tool が EH-3 に 2 回ブロックされ（.sh ファイル + PLANGATE_SKIP_REASON 未設定）、Python heredoc へ切り替え。ta-44 TC-02/03 で PLANGATE_WORKING_DIR が hook 内部計算 REPO_ROOT に無視されることを見落とし、サンドボックス設計を修正（実 docs/working/ 配下の一時 TASK ディレクトリを使う方式に変更）。
- 次回再利用すべき判断: ① EH-3 が .sh 含む非 .md ファイルをブロックするためファイル作成は Python 経由（PLANGATE_HOOK_TASK 設定）② hook スクリプトは REPO_ROOT を内部計算するため hook 単体テストは実 docs/working/ 配下にサンドボックスを作成する ③ apply-script 設計（patch_file old/new + dry-run + already applied 検出）は TASK-0141 から踏襲し差分が少ない ④ extras/ 自動 source（run-tests.sh 編集不要）のパターンを活用
- 効いた skill / gate / artifact: ta-43（TASK-0141）の apply-script パターンを踏襲したことで apply-script 設計コストがゼロ。test 結果 332 passed 0 failed で回帰なし。
- 1 人運用で負荷が高かった箇所: EH-3 ブロックへの対処（Python 経由 heredoc の Python 構文エスケープ問題）。hook スクリプト内 REPO_ROOT 計算がテスト設計に影響する点の検出遅延。
- confirmed_by: masatake.komine

## 2026-08-05 — v8.18.0 リリース + #914 完遂 + Plan Contract 系 pbi 整備 run

- 目的達成可否: 達成（一部 Human 待ち）。v8.18.0 リリース完遂（version 同期マップ 8/8・#950 恒久対応として Actions の PR 作成権限を有効化）。#914 を exec 開始から CLOSED まで完走（PR #986 `0ebb8fe`・変異注入 8 件・V-1 独立検査 FAIL 0・River Review 3 回）。pbi-input 6 本（#928 #894 #906 #960 #980 #921）と #981 の Plan Package（plan/todo/test-cases + C-1 3 回 + C-2 2 レーン）を整備。#981 の C-3 と #1003 の C-4 が Human 待ちで停止。
- 失敗・手戻り: オーガナイザー自身の誤りが 5 件。① `apply-claude-settings.sh` を効果未検証のまま 5 ターン繰り返し依頼したが dry-run すると「変更なし」の no-op（スクリプトは 2 分岐しか実装しておらず不足 5 件を配線できない）→ #914 が長時間ブロック ② #921 に「ta-58 の伝播は PR #988 で是正済み」と投稿したが実際は `0ebb8fe`（#986）で、`git show 7680145:...` では 0 件（issue に再訂正投稿）③ `FORBIDDEN_KEYS` を 15 個と計測（`grep -A2` が余分な行を拾った。正は 14 個）④ 追跡 `c3.json` を 81 件と報告（正は 80 件。`PBI-116/parent-c3.json` は exec preflight が読まないため母数外）⑤ ワーカーの worktree を完了報告直後に削除し、修正指示時にワーカーが共有 checkout へ落ちた（2 件・いずれもワーカーが自力復旧）。構造的な手戻りとして、1 本目のレビュー修正が新しい major を作り（PR #976 の apply が特定条件で永久に非収束）2 本目で捕捉。TC-33 の false positive で #914 の CI が 2 回ブロックされ、3 回目で対症療法（新規ファイルの 1 行化）から根本修正（awk による行継続畳み込み）へ切り替えた。
- 次回再利用すべき判断: ① 依頼コマンドは dry-run か実装分岐の確認で効果を実測してから出す。同じ依頼を 2 回繰り返したら依頼内容自体を疑う ② 修正にも次のレビューラウンドを当てる — 「検証は層ごとに深くなる」は修正そのものにも適用され、1 本目の指摘を直した版に 2 本目を当てて初めて非収束バグが出た ③ 対症療法が 2 回続いたら検査側（根本）を直す — ta-59 → ta-60 で同じ false positive が出た時点で切り替えるべきだった ④ 帰属 PR の主張は `git log -S` で裏取りしてから書く ⑤ 数え上げは抽出方法を明示して数える（範囲込み grep のカウントを避ける）⑥ ワーカーの worktree はレビュー通過まで残す。
- 効いた skill / gate / artifact: River Review の多層化（1 本目=実装の穴、2 本目=修正が作った穴、と別クラスを捕捉）。変異注入（#914 の 8 変異 / TC-33 の 3 変異 / ta-59 の 5 TC）が毎回「空振りテストでない」ことを実証。C-2 の 2 レーン責務契約 — コードベース整合レーンが「#874 が `docs/schemas/`（非 HO）という別解を確立済み」を発見し、設計妥当性レーンだけでは出ない指摘を出した。ワーカーが指示の前提を実測で覆す規律が複数回機能（未引用 `${VAR:-}` の word splitting 挙動 / EH-6 の参照先 / レビュアーの行番号誤り / 並行セッションとの衝突を検知して push を止めた判断）。実物照合（head SHA・exit code・一次ソース）が報告の誤りを毎回捕まえた。
- 1 人運用で負荷が高かった箇所: 並行セッションとの衝突が頻発（同一ブランチへの二重投入で ref が 2 回進行 / carry-over の上書き競合 v43〜v60 / 共有 checkout が `main` でなく作業ブランチにいた）。EH-3 配線後に Edit/Write が全面 block され起動時 env でしか解除できずセッション再起動が要る。Human 待ちが同時多発（apply 実行 / #981 C-3 / PR マージ）し、待ちの間に着手できる範囲の見極めにコストがかかった。gh アカウントドリフトが複数回発生（禁止アカウントへの自動切替）。
- confirmed_by: masatake.komine（「confirm」指示にて）

## 2026-09-07 — wave-10 run（#1289 EH-3 回帰網 / #1291 #1263 R3 / #1292 #1257 version bump ゲート）

- 目的達成可否: 達成（一部 Human 待ち）。#1289（`ta-79` 拡張 +421 / `ta-80` 新規 +533 / pending flag 2 本。実測: `git show --numstat 1adbf61f`）と #1291（`ta-82`）をマージ。#1292 は CI 緑で C-4 待ち。HO patch 2 本（#1278 / #1234）の適用可否を再実測し（`git apply --check` 4 通り rc=0・順序不問・結果 `cmp` 一致）Human 向けコマンドを確定。#1293（`ta-62` の実行時間 vs `ta-61` 予算）を起票。エージェント 9 体（実装 3 + 是正 2 + レビュー 4）。
- 失敗・手戻り: オーガナイザー自身の誤りが 4 件で、**4 件とも「単一の観測から原因・規則を断定し、決定要因を測っていない」同じ形**だった。① CI 失敗を「fail-open は macOS 固有」と OS に帰属させ PR コメントに公開（実際は `/bin/sh` の実体依存で、bash-as-sh な Linux でも fail-open。最も危険なケースを安全側に見せた）② CI 赤に対し「rc の数値固定をやめる」と差し戻したが `tests/extras/README.md` の `P-1`/`P-2`（rc と一意トークンの対・選言禁止）に抵触し、#1278 の本質（rc=2 と rc=1 の差）を検査不能にした（ワーカー往復 +1 ラウンド）③ `gh` の 401 で `H=$(gh pr view …)` が空になり `git show "$H:path"` が index を返して**恒真の「変更なし」**を出した（自己検出）④ `ta-61` の診断文字列を設計意図として引用し「予算引き上げでは根治しない」を起票済み issue の受入基準に書いた（本文書き換え + 訂正コメント）。自力検出は ③ のみで、①②④ は並行セッション起点。また派遣文に probe の必須要件 2 点（本体と同じ `sh` 実体で測る / 導出不能なら fail）が欠落していた。成果物側では `ta-82` の初版が critical（静的ピンが `sync_dir` の no-op 再定義・ラベル分岐の早期 return・`PLUGIN_DIR` 付け替えをすべて素通り）で、挙動 TC への作り替えが必要になった。
- 次回再利用すべき判断: ① 環境差の原因帰属は **OS でなく実行系の実体**を列挙して測る。エラーモードごとに別々に測る（リダイレクト失敗は dash で rc=2 だが `mkdir` 失敗は両方 rc=1）② CI 赤を**期待値の緩和**で通す前に repo の判定規約を読み、「この緩和で何が検出できなくなるか」を 1 文で書けないなら緩めない。環境依存の値は定数を捨てず**実行時 probe で測って定数の代わりにする**③ コマンド置換の結果を照合に使うなら**空でないことを先に検査**する。positive control は**検査対象の領域内**を壊す④ コードの**診断文字列を設計意図として引用しない**⑤ 派遣**前**に成果物名（`ta-NN`）の衝突を確認する（`1234-*` §8 と #1257 メモが両方 `ta-80` を想定していた）⑥ 静的ピンで守るときは「その字面を変えずに同じ目的を達成できる経路」を先に数える。
- 効いた skill / gate / artifact: 敵対レビューが**実物照合を超えた** — `ta-82` は私の照合で 10/0 rc=0 の緑だったが、レビュアーがサンドボックスで実スクリプトを走らせて 3 クラスの回避を発見した（緑は何も保証しない）。影響範囲の**独立実測**（`plugin/plangate` に触れた first-parent 99 commit のうち 91 が version 据え置き = ゲートで赤）が A-2'（ゲートをリリース時へ移す）という設計変更を導いた。横断依存の事前検出 — merge-tree 10 組に加え、他レーンの PR が触る patch 文書の `<!-- PG-PATCH-BEGIN -->` ブロックが byte 一致であることを positive control 付きで確認（テキスト衝突ゼロでも壊れる依存を潰した）。並行セッションとの相互訂正がオーガナイザーの誤り 3 件を捕捉。
- 1 人運用で負荷が高かった箇所: `gh` アカウントドリフトが 2 回（PR 作成が `must be a collaborator` で失敗 / `gh api user` が 401 → 恒真照合を誘発）。switch + viewer 検証 + mutation を同一 Bash 呼び出しで atomic に実行する運用が必須。PostToolUse formatter が `.md` 全体を再整形し、既存行（`**PLANGATE_* env …**`）を壊したため、`git checkout origin/main -- <path>` で原本に戻して変更行だけを再適用する必要があった。並走負荷（load1 8〜20）で `ta-62` が 180s 予算を超え、4 名が独立に無関係な FAIL の切り分けコストを払った。HO patch が 2 本 → 4 本に増え、適用順序の正本が別文書（`1226-*` §8-5）に移った。
- confirmed_by: masatake.komine（`/growth-core:engineering-process-improvement` の選択肢「A5件の docs PR + seeds 追記」にて confirm）

## 2026-09-08 — Phase 0/0.1 残件解消 run（#1295〜#1302 / ai-loop V2 canon hardening）

- 目的達成可否: 達成。Phase 0 / 0.1 の exit criteria が**未チェック 0 件**になり Phase 1 設計へ進める状態（`b1217b41`）。7 PR をマージ（#1295 / #1296 / #1297 / #1298 / #1300 / #1301 / #1302）。独立レビューを **I1 ×3**（R1 = docs 整合・敵対 / R2 = 是正の検査）実施し、**artifact として永続化**（`docs/working/_reports/1275-phase0-01-independent-review.md`）。検出は **critical 1 + major 10 + minor 11 + info 2**。新規起票 #1299（`corpus_hash` が enforcement 層を含まない P1）。#1285 の Scope / AC を全面是正。
- 失敗・手戻り: オーガナイザー自身の誤りが 3 件。① **CI 失敗の分類をログを読まずに行い**、`memory-automerge-guard` を「auto-merge 適格性のガードで内容の欠陥ではない」と 5 ターン以上報告し続けた（実際は `name` とファイル名の不一致 8 件 + 必須節の欠落 1 件を含む）② **`!` 実行の結果を次ターンで再検証しなかった** — ユーザーに依頼した `git pull` が 2 回とも効かず、3 回目で原因を切り分けるまで約 3 ターン消費（既存規律「ユーザー操作依存の依頼は次ターン冒頭で実物再検証」の適用漏れ）③ 調査ワーカーの派遣文に**基点 SHA の固定を書かず**、ワーカーが 4 commit 遅れのローカル `main` で調べたため「V2/Legacy の不整合が現存する」という **stale な結論**を受け取った（#1295 で既に是正済み。気づかなければ存在しない問題の是正 PR を出していた）。ワーカー側では**破壊的 git 操作を 1 ウェーブで 2 件**（`git checkout origin/main -- <dir>` で自分の 8 編集を全消し / `git stash` の混入）。いずれも自己申告 + 自力復旧だが、**委託文に「`git stash` 禁止」を明記していたのに踏まれた**。
- 次回再利用すべき判断: ① **是正そのものにレビューを当てる（R2）** — R1 の是正が「`allowed_paths` を塞いだ直後に `verifier set` という別の宣言依存を導入する」形で**同じクラスを再生産**していた。R1 の検査モデル（記述の有無）では原理的に検出できず、**是正が入って初めて「この観測値はどこから来るのか」が問える** ② **「マージ済み ≠ 独立レビュー済み」** — 既存レビューの再利用は「対象 / commit SHA / 独立性 / 指摘対応」の 4 点が確認できたときのみ。確認できないものは**「確認不能」として充足扱いにしない** ③ **レビュー記録は artifact 化し、対象 SHA を必ず添える** — commit message だけの証跡は「どの版を見たか」を失い、次の改変で無言に失効する ④ **禁止は理由と代替手段をセットで書く** — 列挙だけでは 2 件踏まれた ⑤ **CI / gate の失敗は分類の前にログを読む**。分類を報告に書くなら根拠のログ行を添える ⑥ **調査系の派遣は `origin/main` の SHA を実測してから始めさせる**（共有 checkout のローカル `main` は遅れている）。
- 効いた skill / gate / artifact: **観点を直交させた 2 本立てレビュー** — docs 整合は critical 0 判定、敵対は critical 1（保護集合の交差判定が Candidate の自己申告に依存し、`Candidate cannot modify the authority that judges the candidate` が迂回可能）を検出。**1 本だけなら見逃していた**。**`review-principles.md` §7-quater の回避クラス台帳** — 「前ラウンドが見つけられなかった理由」を必須項目にしたことで、R2 の 5 行すべてに**検査モデルの構造的限界**が記録された（「甘かった」ではなく「その次元が検査モデルに無かった」）。**ワーカーがレビュアーの指摘を実測で覆した** — 「4 skill とも `.codex` が正直」を 3 skill に是正し、残り 1 件は性質が違う（正本側の記述不足）と切り分けた。**自分で作ったルールに自分で引っかかる構造** — `reviewed_at_sha` の「差分がレビュー対象の主張に触れるなら再レビュー」が、まさにその是正差分に適用され R2 を強制した。
- ツール・プロセス上の摩擦点: `!` 経由でユーザーに依頼したコマンドの**出力が届かず**、成功/失敗を切り分ける手段が無い（3 ターン消費）。`PLANGATE_SKIP_REASON` 未設定のセッションでは `.md` 以外を書けず、**repo 外の `~/.claude/state/*.json`（ウェーブ台帳）すら EH-3 に block される**（#1234 の実害）ため、再開点を carry-over に頼るしかない。PostToolUse formatter が表を再整形し、2 行の変更が +174 行になった（原本へ戻して変更行だけ再適用）。`gh` アカウントのドリフトが 1 回（PR 作成が `must be a collaborator` で失敗し発覚。それ以前の issue コメント 1 件が別アカウントで投稿されていた）。HO patch が **5 本**に増え、適用順序の正本が `1226-*` §8-5 + §8-5-bis に分散。**Quality Gate 未達 1 件**: 本振り返りの改善 5 件は**すべて A（ドキュメント）で、B（自動ガード）に落とせなかった** — エージェントの操作規律と分類判断の問題で、repo 内のファイル状態を検査しても検出できないため。
- confirmed_by: masatake.komine（`/growth-core:engineering-process-improvement` の選択肢「2 = seeds + A 3 件の docs PR」にて confirm）

## 2026-09-08 — HO patch 段 1 の適用と後続是正（#1304 マージ / #1305 / #1288 drift）

- 目的達成可否: 達成。**EH-3 の残存欠陥 3 件（#1278 fail-open / #1234 repo 外パス / #1226 HO 9→12）が main に入った**（`f455a7b7`）。`_ho_key` / `_phys_key` とも 12 arms、`OUTSIDE_REPO_SKIP` 4 / `audit log unavailable` 3 / pending flag 0 を `origin/main` の実体で確認。適用は Human 実行、PR #1304 として記録。後続の `.codex/skills` drift 解消を #1305（CI 10/10 緑・River Review 済み）で用意。
- 失敗・手戻り: オーガナイザー自身の誤りが 3 件。① **他人の調査報告の記述を差分と照合せず PR 本文へ転記**した（#1288 報告書の「`ai-loop-cycle` は元から同じ主張を持つので悪化しない」。実際は例外注記 0 件のまま `scripts/arbiter.py` の具体パス断定と「cwd 相対で叩くな」の禁止が新規に入り、`.codex` 側に `scripts/` は存在しない）。River Review が検出 ② **`--force` の出力を無検査でコミットしかけた**（`plan-normalization/agents/openai.yaml` は #1221 で意図的に手置きされた値。同じくレビューで検出）③ **「#1104 の patch 2 本は stale・作り直しが必要」と carry-over と PR 本文に書いたが誤り**（ワーカーの実測で判明。実態は A=適用済み / B=当てると重複 / C=patch を含まない設計文書）。さらに④ 自分の B 実装（curated 保護）の初版が「既存値 ≠ 生成値」で判定しており、**canon の description を正当に更新したときも上書きされなくなる**退行を自分の対照実験で検出し、明示マーカー方式へ設計変更した。
- 次回再利用すべき判断: ① **引用は一次証跡の対象外になっていた** — 「進捗は一次証跡で示す」は自分の作業にしか効いておらず、**他人の結論を自分の主張として転記する経路**が無検査だった。転記する主張が現在の差分でも真かを自分で確かめる ② **生成物にも差分レビューを当てる** — 「生成器の出力だから」は、その生成器が触ってよい範囲を確認したときにだけ成り立つ ③ **patch 文書の適用状態は fwd と rev の両方を測り、`--check` rc=0 を「適用可能」と読まない** — B-1 は rc=0 だが `git apply -v` が `offset 10 lines` で**既存ブロックの直後**に当て、同じ配線が 2 つになる。同じ形の並びが複数ある構造では、当たる位置と想定位置が違う ④ **保護・除外の判定を「値の差」で作らない** — 正当な更新と区別できず、機構が事実上停止する。**明示マーカー / 台帳**で意識的な操作を要求する（`ta-71` の GAP-DECL と同型）⑤ **検査の発火条件を数える** — 新設した検査が `pull_request` job にしか無く、main への push では発火しないため「main が壊れても main の CI は緑」だった。
- 効いた skill / gate / artifact: **前回の振り返りで `behavior-norms.md` に入れた「CI / gate の失敗を分類する前にログを読む」がそのまま効いた**（8 件の失敗を 2 原因に正しく分類）— 改善が次の run で回収された初のケース。**`ta-71` の KNOWN-GAP 台帳が「新しい穴を flag で吸収させない」設計どおり機能**し、#1226 patch 自身のバグ（`.codex/skills/**` を `pull_request.paths` にだけ追加し `push.paths` に入れていない非対称）を検出した。flag へ追記せず**非対称そのものを解消**したので flag は無変更。**River Review が実バグを 1 件検出**（上記②）。**ワーカーがオーガナイザーの前提を実測で覆した**（上記③）。**自分の実装に自分で対照実験を当てて退行を検出**（上記④）。
- ツール・プロセス上の摩擦点: **CI 完了前に PR #1304 がマージされ**、drift 10 件が main に残った（検査が `pull_request` にしか無いため main の CI は緑のまま。次に PR を出すまで誰も気づかない構造）。自作の適用スクリプトが **BSD grep で落ちた**（`grep -qF -- "$1"` の `--` 欠落で `- '...'` がオプション扱い。適用は成功し検証だけ失敗、ユーザーに 1 往復の手間）。`gh` アカウントのドリフトが 1 回（PR 作成が `must be a collaborator` で失敗）。当初の B 案（canon description の 64 文字検査）は **40/40 全件が超過**で成立せず、実測してから棄却した。
- confirmed_by: masatake.komine（`/growth-core:engineering-process-improvement` の選択肢「A 2 件 + B 1 件 + seeds（推奨）」にて confirm）

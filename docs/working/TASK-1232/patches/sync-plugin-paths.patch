--- a/.github/workflows/sync-plugin-plangate.yml
+++ b/.github/workflows/sync-plugin-plangate.yml
@@ -12,6 +12,20 @@
       - 'CHANGELOG.md'
       - 'docs/ai/ai-loop/**'
       - 'docs/workflows/ai-loop/**'
+      # sync-plugin-plangate.sh の同梱元 (#1232 / #1249 MAJOR-1 / #1250 R3)。
+      # scripts/sync-plugin-plangate.sh が $REPO_ROOT 経由で読む入力集合と
+      # 一致させること。不足すると「これらだけを変える PR」で drift-check が
+      # 起動せず、配布物が無警告で腐る。機械照合: tests/extras/ta-71 TC-20/22/23。
+      # ※ `docs/*` のような 1 階層 glob に潰さないこと（GitHub の paths: では
+      #   `*` は `/` を跨がないため docs/ai/*.md 等が外れる）。`!` 除外も足さない。
+      - '.claude-plugin/marketplace.json'
+      - 'docs/ai-driven-development.md'
+      - 'docs/plangate.md'
+      - 'docs/ai/core-contract.md'
+      - 'docs/ai/plan-metrics-verification.md'
+      - 'docs/ai/settings-wiring-contract.md'
+      - 'docs/schemas/**'
+      - 'docs/working/templates/**'
       - 'scripts/ai-loop/**'
       - 'scripts/_ai_loop_link_rewrite.py'
       - 'scripts/sync-plugin-plangate.sh'
@@ -22,6 +36,18 @@
       - '.agents/skills/**'
       - 'docs/ai/ai-loop/**'
       - 'docs/workflows/ai-loop/**'
+      # push 側と同一集合に保つこと（片側だけ足すと PR 時の drift-check か
+      # push 後の自動同期のどちらかが盲点として残る）。集合一致は
+      # tests/extras/ta-71 TC-22 が機械照合する。
+      - 'CHANGELOG.md'
+      - '.claude-plugin/marketplace.json'
+      - 'docs/ai-driven-development.md'
+      - 'docs/plangate.md'
+      - 'docs/ai/core-contract.md'
+      - 'docs/ai/plan-metrics-verification.md'
+      - 'docs/ai/settings-wiring-contract.md'
+      - 'docs/schemas/**'
+      - 'docs/working/templates/**'
       - 'scripts/ai-loop/**'
       - 'scripts/_ai_loop_link_rewrite.py'
       - 'scripts/sync-plugin-plangate.sh'

# ai-loop Run-028 — LoopSpec + C-3' 裁定記録（TASK-0970 / #970 経路1 集計-削除 非対称の解消）

> 対象: [`docs/working/TASK-0970/plan.md`](../TASK-0970/plan.md)（C-2 反映済み・簡易 C-1 再実行済み）
> 裁定: **AUTO_APPROVED**（`arbiter.py` exit 0 / priority 6: verdict = approve-approve 合意）
> decision record（正本・逐語）: [`20260805T013823Z-4448420-run028.json`](./20260805T013823Z-4448420-run028.json)
> 基点 `target_sha` = `4448420cb48261aefa9fd274e498f140ab5e4cf7`（= `origin/main`）

## LoopSpec

`derive_loopspec('docs/working/TASK-0970','TASK-0970', maker='claude-opus-5/plan-author',
checker='claude-opus-5/c1-independent-reviewer')` の出力（本 run で再実行して取得）:

```yaml
loop:
  name: plan-first-task-0970
  trigger: {type: manual}
  goal:
    description: |
      経路1（汎用 references）の mass-delete guard において、
      **stale 集計集合と削除ループの削除集合を厳密一致させる**。
      これにより「N 件と数えて M 件消す」guard 無効化（#861 同型）の残存窓を閉じ、
      その対称性を回帰テストで固定する。
    exit_criteria_ref: "docs/working/TASK-0970/test-cases.md"
  context:
    include: [plan_package, diff, test_results]
    exclude: [stale_tool_outputs]
    external_sources: []
  scope:
    allowed_paths:
      - "scripts/sync-plugin-plangate.sh"
      - "tests/extras/ta-26-plugin-sync.sh"
  actors:
    maker: claude-opus-5/plan-author
    checker: claude-opus-5/c1-independent-reviewer
  verification:
    deterministic:
      - cmd: "sh tests/extras/ta-26-plugin-sync.sh"
        expect_exit: 0
      - cmd: "sh tests/run-tests.sh"
        expect_exit: 0
    review: [requirements_fit, architecture_consistency]
  stopping_rule:
    terminal_state_ref: "decision-table.md"
    round_limit_ref: "execution-runbook.md §2-(7)"
  memory:
    write: [decision_record]
    ref: "execution-runbook.md §2-(4)"
  escalation: {touches_ho: unconditional, budget_ref: "arbiter-policy.md §7"}
```

## W チェック（R1 / 2 体とも approve・severity none）

### Model A（順方向）— `approve` / `none`

> issue AC-1〜4 が plan の受入基準へ逐語で写像され、Files to Touch 2 件・禁止領域宣言・
> A-1/A-2 の設計比較根拠が揃っている。L206 削除で集計条件式が削除ループと厳密一致することと、
> TC-35 の期待値（rc=3 / base=3 / stale=5 / 残存 9 / target 非破壊）および M-1 の非空振りを
> sandbox 実走で独立再現した。「実測済み」申告（行番号・TC 数 30・symlink 0 件・-e/-f 挙動）は
> すべて裏取りでき、baseline のみ記号化して evidence へ委ねる扱いも安全側で妥当。

### Model B（逆方向・adversarial）— `approve` / `none`

> sandbox 実走で計画の主張を独立再現した（現行実装 = rc:0・stale:3 と数えて 5 件削除／
> L206 の `-L` 1 行削除後 = rc:3・`base=3 / stale=5`・9 件全残存・symlink target 非破壊）ため
> TC-35 は空振りせず M-1 で確実に FAIL する。base=3/stale=1・3/3・3/4 および dir-symlink・
> 循環 symlink・`sub.md` ディレクトリの各 fixture を修正前後で比較して rc/残存件数が完全一致する
> ことを実測し、既存 TC-29/32/34 への巻き添えと過剰 block が無いことを確認した。src 側 L200 を
> 残す非対称は base を過小評価する方向＝fail-closed のみで新たな fail-open を作らず
> （src symlink と同名の dst ファイルは集計・削除の両方から対称に除外される）、経路2 の
> `[ -f ]` のみの集計と規約が揃うため一貫性はむしろ向上する。

## オーガナイザーの判断記録

1. **非 production run として実行**: `plan_package` ブロック（reviewer snapshot 三つ組等）の
   組み立て仕様が厚く、`ai-loop run TASK-XXXX` の正式入口が CLI に存在しない（**#982**）ため、
   SKILL.md が許容する非 production 経路で裁定した。Plan Package 自体は
   `check_presence` = `[]` / `check_evidence` = `[]` で受理される状態にある（実測済み）。
2. **`changed_files` は plan の Files to Touch（2 件）をそのまま使用**: C-2 の R-001 で
   「申告者がフィルタした集合を機械ガードへ渡すと `size_ok` の機械検証が申告制へ差し戻る」と
   指摘され、plan 側の Files to Touch を 2 件へ是正した結果、正本どおり渡して
   `machine_size_check` を通過した。**実装後の再裁定を行う場合は `git diff` に作業コンテキストが
   入り実数が 2 を超えるため `size_ok=false` を申告すること**（C-1 レビュアーの申し送り）。

## 不変条件（本 run でも緩和しない）

- NO MERGE BY AI / C-4・merge は Human-owned 固定
- HO 接触が判明した時点で無条件 escalate（本 run は `boundary_check = clean` を実測）
- lite 4 軸の AC-8 安全側

# v8.20.0 リリース手順書

> 対象タグ: **`v8.20.0`** / 前回タグ: `v8.19.0`（2026-08-13）
> 基点 main: `0559781`（`git log --oneline v8.19.0..0559781 | wc -l` = **8 コミット**。squash merge 運用のため `--merges` は 0 件。うち `0e9ee8f` は v8.19.0 のリリース後 changelog 同期のため、本リリースの実質的な変更は **7 件**）
> 準備ブランチ: `chore/v8200-release-prep`
> 正本: [`docs/release-process.md`](../../release-process.md)（Iron Law: **NO RELEASE WITHOUT TAG-MAIN PARITY**）
> 責務分界の正本: [`.claude/rules/responsibility-classes.md`](../../../.claude/rules/responsibility-classes.md) §対外公開アーティファクト publish 責務分界

## 0. このリリースの要点

- **Human が実行するのは §4 の Step 1〜9 のみ。上から番号順に、各 Step 内のコードブロックも上から順に実行する**（#1089 の apply は Step 9 = リリース完了後。理由は §5）
- 主題は **「緑を出していた検出器が、実際には別のものを測っていた」箇所の実測是正** — Codex plugin の `registered: YES` false green（#1085 / #1090）・skill frontmatter 破損の 4 root 同時是正（#1084）・Codex CLI parity 記述の「強制力 0/11」への再是正（#1078 系）
- **`schemas/*.json` / `bin/plangate` / `scripts/hooks/*.sh` / `.github/workflows/*` は変更ゼロ**（実測: `git diff v8.19.0..0559781 --stat -- schemas/ bin/ scripts/hooks/ .github/workflows/` が空）→ Schema / CLI / Hook の挙動は不変
- **本リリースは EH-3 の HO 迂回（#1089）を未解消のまま出す**。patch と回帰テストのみ同梱し、hook 本体（HO パス）の適用は Human-owned。CHANGELOG の「⚠️ 既知の未解消ギャップ」が本リリース最重要の告知
- open PR は **0 件**（#1090 / #1091 とも 2026-08-14 に main へマージ済み。準備時点で「入る / 入らない」の分岐は存在しない）

## 1. リリース要否の判定（`docs/release-process.md` §判定サイクル）

| 判定条件 | 実測 | 該当 |
|---|---|---|
| 前回タグから 14 日以上経過 かつ `plugin/` に未リリース変更あり | **1 日**（v8.19.0 = 2026-08-13） | ❌ |
| 導入側の挙動が変わる変更がマージされた | **#1084 で `plugin/plangate/skills/plangate-setup/SKILL.md` の frontmatter 破損を是正** = 導入先で skill が読み込まれない状態の解消 | ✅ |
| 機能単位の区切り | #1078 系 Codex parity 是正の収束（#1080 / #1082 / #1083 / #1088） | ✅（弱） |
| 見送る（`plugin/` 未リリース変更 0 かつ doc-only） | `plugin/` 配下 4 ファイル変更あり | ❌ |

**結論: 出す。** 経過日数は基準未満だが、**配布物（plugin skill）の破損是正が入っている**ため「導入側の挙動が変わる変更」に該当する。日数トリガーは満たさないので、この判定根拠を Human が却下する場合は次回機能リリースへの同梱に切り替えてよい（その場合 §3 は実行せず、本手順書を次リリースへ引き継ぐ）。

## 2. semver 裁定の材料（決定は Human-owned）

**AI の提示: `8.20.0`（minor）**。[`docs/ai/versioning-stability-policy.md`](../../ai/versioning-stability-policy.md) §2 に照らした材料:

| 軸 | 実測 | semver 材料 |
|---|---|---|
| 破壊的変更 | `schemas/` / `bin/plangate` の差分 **0 バイト** | major 材料なし |
| Enforcement Hook の既定挙動変更 | `scripts/hooks/*.sh` の差分 **0 バイト**（#1091 は patch + テストのみで hook 本体を変更しない） | major 材料なし |
| 新規 hook の追加 | なし | major 材料なし |
| 機能追加（後方互換） | `plugin/plangate/.codex-plugin/plugin.json`（新規配布マニフェスト）・`scripts/check-plugin-manifest-parity.sh`・`scripts/check-skill-frontmatter.py`・`scripts/apply-eh3-ho-always.sh`・`tests/extras/ta-64` / `ta-65` / `ta-66` | **minor** |
| バグ修正のみ | いいえ（上記の additive 追加を含む） | patch では不足 |

→ **minor（8.20.0）**。v8.19.0 のような規約 vs 裁定の食い違いは本リリースには存在しない。

## 3. version bump の状況

正本は [`docs/release-process.md`](../../release-process.md) §version 同期マップ。

| # | ファイル | 現在値 → 新値 | 担当 | 状態 |
|---|---|---|---|---|
| 1 | `CHANGELOG.md` | `## v8.20.0 (2026-08-14)` 節を追加（`## Unreleased` は残す） | AI | ✅ 実施済 |
| 2 | `plugin/plangate/.claude-plugin/plugin.json` L3 | `8.19.0` → `8.20.0` | AI | ✅ 実施済 |
| 3 | `plugin/plangate/.codex-plugin/plugin.json` L3 | `8.19.0` → `8.20.0` | AI | ✅ 実施済（#1085 で新設。**parity 検査対象**） |
| 4 | `.claude-plugin/marketplace.json` L9（`metadata.version`）/ L15（`plugins[].version`） | `8.19.0` → `8.20.0`（**2 箇所**） | AI | ✅ 実施済 |
| 5 | `README.md` L81（冒頭散文）/ L87（最新リリース行） | `v8.19.0` → `v8.20.0` | AI | ✅ 実施済 |
| 6 | `README_en.md` L81 / L87 | `v8.19.0` → `v8.20.0` | AI | ✅ 実施済 |
| 7 | `plugin/plangate/README.md` L6 | `v8.19.0` → `v8.20.0` | AI | ✅ 実施済 |
| 8 | **`CLAUDE.md`「最新リリース」節** | `v8.19.0` → `v8.20.0` | **Human** | ⬜ **未適用**（§4 Step 2。apply スクリプト `scripts/apply-claude-md-v8200.sh` は AI 作成済） |
| 9 | `docs/changelog.md` | **リリース前に同期済み**（`sh scripts/sync-release-docs.sh`） | AI | ✅ 実施済 — `release-prep.sh --check` の `check_changelog_sync()` が `sync-release-docs.sh` の `no-op` を要求するため、**同期しないと READY にならない**。release published 後の `release-docs-sync` 自動 PR は差分ゼロ（no-op）になる。run 結果の確認は §4 Step 7 |

### bump 不要と判定した箇所（実測して除外）

`git grep -n '8\.19\.0'` の全ヒットのうち、以下は **v8.19.0 という過去世代を指す固定記述**であり bump しない:

| ファイル | 理由 |
|---|---|
| `docs/ai/settings-wiring-contract.md` L409 | 「v8.19.0 で fail-closed 化された EH-13」という史実の記述 |
| `docs/release-process.md` L34 / L77 | v8.19.0 runbook を実例として参照。L34 の「実例」リンクは本 runbook へ差し替えてもよい（任意・P2） |
| `scripts/ai-loop/test_run_evidence.py` L460 / `plugin/plangate/skills/ai-loop-cycle/scripts/test_run_evidence.py` L460 | TC-31 の **drift 検出用ダミー値**（`plugin_version` を意図的にずらす）。実 version と一致させてはいけない |
| `scripts/apply-claude-md-v8190.sh` | 過去世代の apply スクリプト（一回性・履歴として残す） |

### 「リリース済 / Shipped」行の欠落是正（本 PR で実施）

`README.md` L88 / `README_en.md` L88 の「リリース済 / Shipped」行は **JA / EN で欠落世代が異なっていた**（v8.19.0 runbook §2 note では 1 つの残件として扱われていたが、実測すると非対称）:

| ファイル | 修正前の最終記載 | 欠落していた世代 | 件数 |
|---|---|---|---|
| `README.md`（JA） | v8.15.0 | v8.16.0 / v8.17.0 / v8.17.1 / v8.18.0 / v8.19.0 | **5 世代** |
| `README_en.md`（EN） | v8.17.1 | v8.18.0 / v8.19.0 | **2 世代** |

**本 PR で両方を埋めた**（1 行の追記のため別 PBI にせず同 PR で処理）。修正後は JA / EN とも v8.7.0 〜 v8.19.0 が連続する（実測: 各行の `v8.x.y` 抽出が両ファイルで同一の 14 要素）。v8.20.0 自身は「最新リリース」行に載るため本行には追加しない（次リリース時に追加する）。

### 機械検証

`tests/extras/ta-28-plugin-version.sh` が #2〜#4 を機械検査する（**extras は共有ハーネス経由でのみ実行できる**。単体起動は契約違反として全 TC が FAIL する — 実行は `sh scripts/run-tests-safe.sh` または `sh tests/run-tests.sh`）。`tests/extras/ta-66-codex-plugin-manifest.sh` が #2 と #3 の parity を検査する。#1 / #5 / #6 / #7 / #8 は未カバーのため本表がレビュー観点の担保。

## 4. リリース手順

各ステップの担当を明記する。**AI は #1〜#7 の bump と CHANGELOG / release note 整備・本手順書・apply スクリプト提示まで。merge / HO 適用 / tag push / Release 作成は Human-owned。**

> 🔴 **番号順に上から実行する。** 各 Step 内のコードブロックも上から順に実行する前提で並べてある（警告・前提条件は必ずコマンドより**前**に置いている）。

### Step 1: 準備 PR の作成・レビュー・マージ 👤 Human

準備ブランチ `chore/v8200-release-prep` は push 済み（PR は未作成 — 本タスクの制約により AI は PR を作らない）。

```sh
gh pr create --base main --head chore/v8200-release-prep --title "chore(release): v8.20.0 リリース準備（CHANGELOG / version bump / runbook）" --body "v8.20.0 のリリース準備。詳細は docs/working/_merge/v8.20.0-release-runbook.md を参照。"
```

C-4 レビュー後、main へマージする。マージ後、ローカルを main の最新に合わせる（3 コマンドを個別に実行する）:

```sh
git checkout main
```

```sh
git pull --ff-only origin main
```

```sh
git rev-parse origin/main
```

### Step 2: `CLAUDE.md`「最新リリース」節の適用 👤 Human（HO パス）

> 🔴 **`main` へ直接 commit / push しないこと。** Step 1 の末尾で `main` にいるため、**必ず下記の 1 番目（ブランチ作成）から順に実行する**。以下は**上から順にそのまま実行できる完全な手順**であり、途中に別のコマンドを挟まない。

`CLAUDE.md` は Hardening Override 対象のため **AI は編集できない**。AI が作成した apply スクリプトを Human が実行する。

#### 1. `origin/main` から作業ブランチを切る

```sh
git fetch origin
```

```sh
git checkout -b docs/claude-md-v8200 origin/main
```

```sh
[ "$(git rev-parse --abbrev-ref HEAD)" = "docs/claude-md-v8200" ] || { echo "ABORT: wrong branch"; exit 1; }
```

#### 2. 差分を確認してから適用する

```sh
sh scripts/apply-claude-md-v8200.sh --dry-run
```

```sh
sh scripts/apply-claude-md-v8200.sh --apply
```

#### 3. commit して push する

```sh
git add CLAUDE.md
```

```sh
git diff --cached --stat
```

```sh
git commit -m "docs(release): CLAUDE.md の最新リリース節を v8.20.0 へ更新"
```

```sh
git push origin HEAD
```

#### 4. PR を作成し、C-4 レビュー後に main へマージする

```sh
gh pr create --base main --head docs/claude-md-v8200 --title "docs(release): CLAUDE.md の最新リリース節を v8.20.0 へ更新" --body "version 同期マップ #8（HO パス）。apply スクリプト scripts/apply-claude-md-v8200.sh の適用結果。"
```

#### 5. マージ後、ローカルを main の最新に合わせる

```sh
git checkout main
```

```sh
git pull --ff-only origin main
```

### Step 3: annotated tag の作成 👤 Human

> 🔴 **前提条件（必ず満たしてから実行する）**: **`scripts/apply-eh3-ho-always.sh --apply`（#1089）を実行していないこと。**
> 実行してしまった場合は **tag を打つ前に §5 の再判定手順**に従い、CHANGELOG / README / README_en / semver を作り直すこと。
> 理由は §5 を参照（この tag に hook 挙動変更が入ると、「hook 本体は未適用のまま出荷」という CHANGELOG / README の記述と、
> 「hook 差分 0 バイト → minor」という §2 の semver 裁定根拠が同時に無効になる）。

```sh
[ "$(git rev-parse --abbrev-ref HEAD)" = "main" ] || { echo "ABORT: not on main"; exit 1; }
```

```sh
git fetch origin main
```

```sh
[ "$(git rev-parse HEAD)" = "$(git rev-parse origin/main)" ] || { echo "ABORT: HEAD != origin/main"; exit 1; }
```

```sh
git diff --quiet v8.19.0 HEAD -- scripts/hooks/ && echo "OK: hook 差分ゼロ（CHANGELOG / semver の前提を満たす）" || { echo "ABORT: scripts/hooks/ に差分あり。§5 の再判定手順へ"; exit 1; }
```

```sh
git tag -a v8.20.0 -m "release v8.20.0"
```

### Step 4: tag の push 👤 **Human**

```sh
git push origin v8.20.0
```

### Step 5: 🚪 Iron Law — TAG-MAIN PARITY 検証 👤 Human（**Release 作成前に必須**）

```sh
sh scripts/check-tag-main-parity.sh v8.20.0
```

- `OK: tag 'v8.20.0' (origin 実体) = origin/main (<sha>)` → **Step 6 へ進んでよい**
- `MISMATCH` / `FAIL` → **GitHub Release を作成してはならない**。[`docs/release-process.md`](../../release-process.md) §失敗時のフロー に従い `--force-with-lease=refs/tags/v8.20.0:<リモート tag オブジェクト SHA>` で貼り替え、**一致するまで再検証**する（MISMATCH 時は script がそのコマンドを出力する）
- 実行ログは本手順書または release note に貼って記録する

### Step 6: GitHub Release の発行 👤 **Human**（Step 5 が OK になるまで実行しない）

release note `docs/working/_reports/v8.20.0-release-note.md` は **本 PR に同梱済み**（AI-owned）。まず実在を確認する:

```sh
[ -f docs/working/_reports/v8.20.0-release-note.md ] || { echo "ABORT: release note が無い"; exit 1; }
```

```sh
gh release create v8.20.0 --title "v8.20.0 — 配布経路の false green 是正（Codex plugin / skill frontmatter）+ Codex CLI parity の実測化" --notes-file docs/working/_reports/v8.20.0-release-note.md
```

> release note は **`CHANGELOG.md` の v8.20.0 節から機械抽出したもの**で、手で書き足していない（二重管理を作らないため）。CHANGELOG を修正した場合は §8「release note と CHANGELOG の一致確認」の手順で作り直すこと。「⚠️ 既知の未解消ギャップ（EH-3 / #1089）」節は Release 本文に**必ず**含める。

### Step 7: リリース後の workflow run 結果確認 👤 Human（#950）

`release-docs-sync` は release published 起点で動くが、**失敗しても通知されない**。v8.17.0 / v8.17.1 / v8.18.0 で 3 連続失敗し `docs/changelog.md` が 2 世代欠落した実績があるため、必ず確認する。

```sh
gh run list --workflow=release-docs-sync.yml --event=release --limit 1
```

- `conclusion = success` → 完了（version 同期マップ #9 は release published 後の自動 PR が **no-op** になる。本 PR で同期済みのため）
- `conclusion = failure` → [`docs/release-process.md`](../../release-process.md) §失敗時のリカバリ に従い、push 済みの `chore/release-docs-sync-<run_id>` ブランチから手動で PR を作成する（`--title` と `--body` を**両方**指定する）

あわせて plugin sync 側も確認する:

```sh
gh run list --workflow=sync-plugin-plangate.yml --event=push --limit 1
```

- `conclusion = success` → 完了（`plugin/plangate/` の配布同期が通っている）
- `conclusion = failure` → **plugin の配布物が更新されていない**。run のログを開いて原因を確認し、修正 PR を出すまで v8.20.0 の plugin 導入は旧版のままになる
- run が 1 件も出ない → 本 workflow は `push` トリガーのため、リリース準備 PR のマージ commit で発火しているはず。発火していなければ workflow の trigger 条件を確認する

### Step 8: ローカル plugin キャッシュの同期 👤 Human（リリース**後**・ローカル専用）

```sh
sh scripts/sync-plugin-installed.sh
```

> `~/.claude/plugins/` と `~/.codex/skills/` を同期する **ローカル実行専用**の作業（CI は `$HOME` にアクセスできない）。**リリース前に実行しても 8.19.0 のキャッシュを同期するだけで意味がない**（§7 の検出器所見を参照）。

### Step 9: EH-3 HO 迂回（#1089）の適用 👤 Human（HO パス / **リリース完了後に実施**）

> 🔴 **Step 3（tag 作成）より前に実行してはならない。** 理由は §5 を参照。

**判断材料**:

| 観点 | 内容 |
|---|---|
| 実害 | `PLANGATE_HOOK_TASK` 設定時、HO 9 カテゴリすべてで EH-3 の block が発火しない（実測 9/9 で `rc=2` → `rc=0`）。`check-forbidden-files.sh` は HO パスを守らないため、EH-3 が唯一の HO ガード |
| 対象 | `scripts/hooks/check-plan-hash.sh` / `.claude/rules/mode-classification.md`（いずれも **HO パス**）+ `tests/fixtures/eh3-known-gap-1089.flag` の削除 |
| リリースへの影響 | **配布物には影響しない**（`scripts/hooks/*.sh` は plugin bundle 外）。本リポジトリを clone して hooks を配線している環境のローカル運用改善 |
| 実施時期 | **リリース完了後**（§5 の理由）。ただし**適用まで本リポジトリの HO ガードは無効のまま**なので、Step 7 完了後は速やかに実施する |

`--dry-run` の実測結果（本準備で確認済み・書き込みなし）:

```text
[apply-eh3-ho-always] mode=dry-run
  (1) check-plan-hash.sh        : WILL CHANGE
  (2) mode-classification.md    : WILL CHANGE
  (3) eh3-known-gap-1089.flag   : WILL DELETE
```

`tests/fixtures/eh3-known-gap-1089.flag` の削除は **apply スクリプトが `git rm -q -f`（失敗時 `os.remove`）で自動的に行う**（別途の削除操作は不要）。

#### 1. `origin/main` から作業ブランチを切る

```sh
git fetch origin
```

```sh
git checkout -b fix/1089-eh3-ho-apply origin/main
```

```sh
[ "$(git rev-parse --abbrev-ref HEAD)" = "fix/1089-eh3-ho-apply" ] || { echo "ABORT: wrong branch"; exit 1; }
```

#### 2. 差分を確認してから適用する

```sh
sh scripts/apply-eh3-ho-always.sh --dry-run
```

```sh
sh scripts/apply-eh3-ho-always.sh --apply
```

#### 3. テストで検証する

```sh
sh tests/run-tests.sh
```

#### 4. commit して push し、PR を作成する

```sh
git add -A scripts/hooks/check-plan-hash.sh .claude/rules/mode-classification.md tests/fixtures/eh3-known-gap-1089.flag
```

```sh
git diff --cached --stat
```

```sh
git commit -m "fix(hooks): #1089 EH-3 の HO 判定を TASK 文脈でも評価する（apply 適用）"
```

```sh
git push origin HEAD
```

```sh
gh pr create --base main --head fix/1089-eh3-ho-apply --title "fix(hooks): #1089 EH-3 の HO 判定を TASK 文脈でも評価する" --body "scripts/apply-eh3-ho-always.sh --apply の適用結果。known-gap フラグを削除し ta-65 の期待値を fixed に戻す。"
```

#### 5. マージ後の追従（次リリースの準備担当への申し送り）

Step 9 のマージ後、以下は **v8.20.0 の記述としては正しいまま**だが、**「現在の状態」としては古くなる**。次リリース（v8.21.0）の準備時に更新すること:

| 対象 | 現在の記述 | 次リリースでの扱い |
|---|---|---|
| `CHANGELOG.md` v8.20.0 §⚠️ 既知の未解消ギャップ | 未解消として記載 | **v8.20.0 節は履歴なので変更しない**。v8.21.0 節の `### Fixed` に「#1089 EH-3 の HO 迂回を解消」を追加する |
| `CHANGELOG.md` v8.20.0 §Notes 1 行目 | 「hook 本体未適用のまま出荷」 | 同上（履歴として残す） |
| `README.md` L81 / L87・`README_en.md` L81 / L87 | 「#1089 は未解消のまま出荷」 | **v8.21.0 の bump 時に当該句を削除する**（README は現在の状態を書く面のため） |

## 5. Step 9（#1089 適用）を tag より後ろに置いた理由

**採用: 「リリース完了後に実施」（Step 9 へ後置）。**

`scripts/apply-eh3-ho-always.sh --apply` は `scripts/hooks/check-plan-hash.sh` を変更する。この変更が main に入った状態で tag を打つと、**本リリースの 6 箇所の記述が同時に嘘になる**:

| 崩れるもの | 場所 |
|---|---|
| 「hook 本体の修正は含まれません」 | `CHANGELOG.md` §⚠️ 既知の未解消ギャップ |
| 「EH-3 HO 迂回は hook 本体未適用のまま出荷」 | `CHANGELOG.md` §Notes |
| 「`scripts/hooks/*.sh` は本リリースで変更ゼロ」 | `CHANGELOG.md` リード文 / `README.md` L81 / `README_en.md` L81 |
| 「EH-3 の HO 迂回（#1089）は未解消のまま出荷」 | `README.md` L87 / `README_en.md` L87 |
| 「`scripts/hooks/*.sh` の差分 0 バイト → major 材料なし」 | 本 runbook §2 の semver 裁定根拠 |
| 「EH-x の既定挙動の変更」＝ major | `docs/ai/versioning-stability-policy.md` §2.2（該当すると 8.20.0 minor が成立しなくなる） |

**却下した案 (b)（番号を外して Step 3 の前提条件だけで縛る）**: 前提条件の明文化は採用したが（Step 3 冒頭 + `git diff --quiet v8.19.0 HEAD -- scripts/hooks/` の機械チェック）、**それ単独では順守が読み手の注意力に依存する**。手順書に実行可能なコマンド一式が並んでいる限り、番号順に実行される前提で設計するべきと判断した。

**この選択の不利益（明示）**: Step 7 完了まで **本リポジトリの HO ガードは無効のまま**である。ただしこのギャップは #1089 起票時点から継続しており、リリース手続き分（数時間オーダー）の延長で増える露出は限定的かつ有界。一方で「未適用と告知したものが実際は適用済み」という**不可逆な対外成果物（tag / GitHub Release）の不整合**は事後に訂正できない。露出時間の有界な延長を選んだ。

**先に適用してしまった場合の再判定手順**:

1. `git diff v8.19.0 HEAD -- scripts/hooks/` で実際の差分を確認する
2. `CHANGELOG.md` の「⚠️ 既知の未解消ギャップ」節を **Fixed の項目へ書き換える**
3. `CHANGELOG.md` リード文 / `README.md` L81・L87 / `README_en.md` L81・L87 の「変更ゼロ」「未解消のまま出荷」を削除する
4. 本 runbook §2 の semver を **`versioning-stability-policy.md` §2.2（EH-x の既定挙動の変更 = major）に照らして再判定**し、Human 裁定を記録する（v8.19.0 runbook §1 と同じ形式）
5. version 番号が変わる場合は §3 の bump を全箇所やり直す

## 6. 担当サマリ

| Step | 内容 | 担当 |
|---|---|---|
| 0 | CHANGELOG v8.20.0 エントリ / version bump 7 箇所 / release note / apply スクリプト作成 / 本手順書 | **AI** ✅ 実施済 |
| 1 | 準備 PR の作成・C-4・main へマージ | **Human** |
| 2 | `CLAUDE.md`「最新リリース」節の適用（HO）+ PR・マージ | **Human**（AI は apply スクリプト提示まで） |
| 3 | `git tag -a v8.20.0`（**hook 差分ゼロの前提チェック付き**） | **Human** |
| 4 | `git push origin v8.20.0` | **Human** |
| 5 | `sh scripts/check-tag-main-parity.sh v8.20.0`（Iron Law） | **Human** |
| 6 | `gh release create v8.20.0`（release note は同梱済み） | **Human** |
| 7 | `release-docs-sync` / `sync-plugin-plangate` の run 確認 | **Human** |
| 8 | `sh scripts/sync-plugin-installed.sh`（ローカルキャッシュ） | **Human** |
| 9 | EH-3 HO 迂回（#1089）の適用（HO）+ PR・マージ | **Human**（AI は `--dry-run` まで） |

## 7. READY 判定チェックリスト（`release-prep.sh --check` の所見付き）

以下を上から順に確認する。

- [ ] `sh scripts/release-prep.sh --check` の `plugin version 一致` が **8.20.0**
- [ ] `plugin マニフェスト整合（.claude-plugin / .codex-plugin）` が OK
- [ ] `docs/changelog.md 同期済み` が OK
- [ ] `sh tests/run-tests.sh` が 0 failed
- [ ] `sh tests/hooks/run-tests.sh` が 0 failed
- [ ] README / README_en の「最新リリース」行と冒頭散文が v8.20.0
- [ ] README / README_en の「リリース済 / Shipped」行が v8.19.0 まで連続している
- [ ] `docs/working/_reports/v8.20.0-release-note.md` が実在し、§8 の一致確認が `MATCH`
- [ ] `CLAUDE.md` の最新リリース節が v8.20.0（§4 Step 2 完了後）
- [ ] `git diff --quiet v8.19.0 HEAD -- scripts/hooks/` が真（#1089 の apply を **tag 前に実行していない**）
- [ ] §4 Step 5（TAG-MAIN PARITY / Iron Law）が OK

### `release-prep.sh --check` の 2 つの NG は「本物の未完了」ではない

本準備で実測した結果、`--check` が返す NG 2 件はいずれも **v8.20.0 のリリースを止める理由にならない**。

#### NG-1「適用待ち apply あり」— 2 本が誤検出、4 本は doc 追記 / 既知の Deferred、1 本は本リリースの正規手順

`check_pending_applies()` は各 `scripts/apply-*.sh --dry-run` の**標準出力に `[dry-run]` という文字列が含まれるか**だけで判定する（`scripts/release-prep.sh` L51-61）。この判定は以下の 3 経路で実態とずれる:

| # | スクリプト | 実測結果 | 判定 |
|---|---|---|---|
| 1 | `apply-ai-loop-workflow-command.sh` | 差分は**逆方向**。コピー元 `docs/working/_prompts/ai-loop-workflow-command.md` は 2026-07-08（#778）で止まっており、適用先 `.claude/commands/ai-loop-workflow.md` は 2026-07-24（#912）。**適用すると 2 週間分の内容が退行する** | ❌ **誤検出 + 適用有害** |
| 2 | `apply-task-0146-ehs23-wiring.sh` | EHS-3 は `[SKIP] already applied`、EHS-2 は `[ERROR] target string not found` だが **`bin/plangate` L2248-2263 に `# EHS-2 (TASK-0146 / #527)` 付きで既に実装済み**。`[dry-run]` は無条件のヘッダ行 | ❌ **誤検出（両方とも適用済み）** |
| 3 | `apply-quality-command-gate.sh` | `.claude/rules/working-context.md` に「品質コマンド実行証跡ゲート」節が不在（grep 実測 0 件）。**未適用**。ただし内容は正本 `docs/ai/quality-command-evidence.md`（実在）への**相互参照の追記のみ** | ⚠️ 真の未適用（doc 追記・リリース影響なし） |
| 4 | `apply-reviewer-silence-gate.sh` | `.claude/rules/review-principles.md` / `docs/ai/gate-checks.md` への**相互参照 2 行の追記のみ**。正本 `docs/ai/reviewer-silence-fallback.md` は実在 | ⚠️ 真の未適用（doc 追記・リリース影響なし） |
| 5 | `apply-ui-v1-crossref.sh` | `.claude/rules/mode-classification.md` への**相互参照 1 行の追記のみ**（grep 実測 0 件） | ⚠️ 真の未適用（doc 追記・リリース影響なし） |
| 6 | `apply-task-0123-patches.sh` | TASK-0123 Part B（maintenance token の HMAC 署名層）。`docs/working/TASK-0123/current-state.md`（2026-07-05）に **「Part A ガード deployed / Part B は適用 or Superseded クローズの Human 判断待ち」** と記録済み。Part A（EH-13 = `scripts/check-approval-token-write.sh`）は稼働済み。本スクリプトが作ろうとする `scripts/hooks/check-approval-token-write.sh` は **実装済みの `scripts/check-approval-token-write.sh` とは別パスで、superseded** | ⚠️ 既知の Deferred（1 か月以上前から Human 判断待ち） |
| 7 | `apply-claude-md-v8200.sh` | **本準備で AI が新規作成したもの**。§3 の version 同期マップ #8（`CLAUDE.md` の最新リリース節）を Human が適用するための正規手順 | ✅ **正しい検出**（§4 Step 2 で適用する） |

> **加えて `apply-precompact-guard.sh` は測定環境で結果が変わる**。通常 checkout では `[dry-run]` を出して「適用待ち」に載るが、worktree（`.claude/settings.json` 未配置）では `ERROR: .claude/settings.json not found` を返し、`check_pending_applies()` の `2>/dev/null || true` に握り潰されて**リストから消える**（fail-open）。同じ検査が実行場所によって別の答えを返す。
>
> **さらに、本当に適用が必要な `scripts/apply-eh3-ho-always.sh`（#1089）は「適用待ち」リストに現れない**。同スクリプトの `--dry-run` は `[apply-eh3-ho-always] mode=dry-run` と `WILL CHANGE` を印字するが、リテラル `[dry-run]` を出さないため grep に掛からない。**リリースを止めるべき唯一の未適用項目が、検出器からは不可視**という状態になっている（実測: 上記 `--check` 出力の「適用待ち apply」7 本に `apply-eh3-ho-always.sh` は含まれていない）。

**結論**: NG-1 に **リリースを止める項目は 1 件も含まれない**。#3〜#5 は doc の相互参照追記（HO パスのため Human 適用）、#6 は 1 か月以上前から記録済みの Human 判断待ち、#1〜#2 は誤検出、#7 は §4 Step 2 で実施する正規手順。

#### NG-2「plugin キャッシュ未同期」— リリース**後**のローカル作業

`check_plugin_cache_sync()` が呼ぶ `scripts/sync-plugin-installed.sh` の同期先は `${HOME}/.claude/plugins/` と `${HOME}/.codex/skills/`（同スクリプト L49-50 / L102）で、**リポジトリの外**。スクリプト自身のヘッダも「**リリース後**にローカルの `~/.claude/plugins/` キャッシュと…」と明記している。

**リリース前の READY 条件に置くのが誤り**（リリース前に実行しても旧 version のキャッシュを同期するだけ）。§4 Step 8 として**リリース後**に移した。

### `release-prep.sh` への是正提案（非 HO・follow-up）

`scripts/release-prep.sh` は HO パス外のため AI が修正できるが、**本リリースのスコープを広げないため本準備では変更していない**。別 PBI として以下を提案する:

| # | 是正 | 理由 |
|---|---|---|
| A | `check_pending_applies()` を fail-open から fail-closed へ。`--dry-run` の **exit code を評価**し、非 0 なら `NG: apply スクリプトが --dry-run に失敗（判定不能）` を出す | 現状 `2>/dev/null \|\| true` で ERROR が「適用待ちなし」に化ける |
| B | 各 `apply-*.sh` に **「適用済みなら pending マーカーを出さない / 未適用なら必ず出す」契約**を課し、契約テストで検査（`apply-claude-md-v8190.sh` の `SKIP: 適用済み` が既存の good pattern）。マーカーは stdout の grep ではなく **専用 exit code**（例: 0=適用済み / 10=未適用 / それ以外=判定不能）で返す | 無条件ヘッダで `[dry-run]` を出すスクリプトが誤検出（#2）、リテラル `[dry-run]` を出さないスクリプト（`apply-eh3-ho-always.sh`）が**検出漏れ**を生む |
| C | `check_plugin_cache_sync()` を READY 条件から外し、**リリース後 TODO** として印字する | 同期先が `$HOME` 配下でリポジトリ外。リリース前に満たす意味がない |
| D | apply スクリプトに **コピー元 / 適用先の新しさ検証**を入れるか、`docs/working/_prompts/` 由来の一回性スクリプトを削除する | #1 の退行リスク（#778 で 17 本削除した際の取りこぼし） |
| E | `vX.Y.Z` モードの CHANGELOG 見出し書式 `## v{v} - {date}` を、既存の `## vX.Y.Z (YYYY-MM-DD)` へ揃える | 現行 `release-prep.sh` L110 は既存 CHANGELOG と異なる書式を生成する |

## 8. 検証結果（実行済み / 2026-08-14・準備ブランチ `chore/v8200-release-prep` 上）

| コマンド | 結果 | exit code |
|---|---|---|
| `sh tests/run-tests.sh` | **740 passed, 0 failed**（version bump + レビュー是正の適用後、最終ツリーで再実行） | 0 |
| `sh tests/hooks/run-tests.sh` | **79 passed, 0 failed**（同上） | 0 |
| `sh scripts/release-prep.sh --check` | version 8.20.0 一致 / マニフェスト整合 OK / changelog 同期 OK。NG 2 件は §7 の分析どおり | 1（NOT READY。ただし §7 の判定により**リリース阻害なし**） |

`sh scripts/check-plugin-manifest-parity.sh` を bump 後のツリーで単独実行し、`OK name=plangate version=8.20.0 skills=skills`（exit 0）を実測した。

> **注意**: `tests/extras/ta-28-plugin-version.sh` TC-08 は **実 tracked file の `.claude-plugin/marketplace.json` を一時改変して復元する**（同ファイル L80-98）。テストスイート実行中に同ファイルを編集・読み取りすると競合する。**version bump とテスト実行は同時に行わないこと**（本準備で実際に競合し、bump の再実施が必要になった）。

### release note と CHANGELOG の一致確認

`docs/working/_reports/v8.20.0-release-note.md` は **`CHANGELOG.md` の `## v8.20.0` 節からの機械抽出物**であり、手で書き足していない（二重管理を作らないため）。一致は以下で確認できる:

```sh
python3 -c "import re;s=open('CHANGELOG.md',encoding='utf-8').read();b=re.search(r'^## v8\.20\.0 \([0-9-]+\)\n(.*?)(?=^## v)',s,re.S|re.M).group(1).strip();n=open('docs/working/_reports/v8.20.0-release-note.md',encoding='utf-8').read();print('MATCH' if b in n else 'MISMATCH')"
```

- `MATCH` → release note は CHANGELOG の当該節を**逐語で内包**している
- `MISMATCH` → CHANGELOG を修正したのに release note を再生成していない。§4 Step 6 の前に再生成すること

**再生成手順**（CHANGELOG を修正した場合）: 上記抽出結果を先頭 4 行のヘッダ（タイトル + 出典 blockquote）に続けて書き出す。ヘッダは以下で固定:

```text
# PlanGate v8.20.0

> このファイルは `CHANGELOG.md` の `## v8.20.0` 節からの**機械抽出物**です（生成: `scripts` 外の一回性スクリプト。手で書き足さないこと）。
> 正本は [`CHANGELOG.md`](../../../CHANGELOG.md)。CHANGELOG を修正したら本ファイルを再生成してください。
```

### markdownlint

`npx markdownlint-cli2` を変更した markdown 7 本に実行。**本準備で追加・変更したファイルの指摘は 0 件**（`v8.20.0-release-runbook.md` / `v8.20.0-release-note.md` / `CHANGELOG.md` / `README.md` / `plugin/plangate/README.md` はいずれも clean）。残る 2 件は base（`origin/main`）由来の既存指摘で、本準備の scope 外（**`git show origin/main:<file>` を取り出して同一指摘が出ることを実測**）:

| ファイル | 指摘 | 由来 |
|---|---|---|
| `docs/changelog.md:6` | MD012 multiple blanks | `scripts/sync-release-docs.sh` の生成物。`origin/main` の同ファイルでも同一指摘が出る（実測） |
| `README_en.md:29` | MD051 link fragments | 本準備で触っていない行 |

## 9. 制約（本リリース準備で守ったこと）

- `git tag` は**作成していない**。tag の push も**していない**
- `gh release create` は**実行していない**
- `git merge` / `gh pr merge` は**実行していない**。**PR も作成していない**
- `scripts/apply-*.sh` は **`--dry-run` のみ実行**。`--apply` は 1 本も実行していない
- HO パス（`CLAUDE.md` / `.claude/rules/*.md` / `.claude/settings*.json` / `.claude/commands/*.md` / `.claude/agents/*.md` / `scripts/hooks/*.sh` / `bin/plangate` / `schemas/*.schema.json` / `.github/workflows/*` / `AGENTS.md`）には**一切触れていない**
- `approvals/*.json` / 承認トークン系ファイルは**作成していない**
- `.codex/` は**変更していない**
- `docs/working/TASK-1078/` は**変更していない**（別ワーカーが是正中）
- release note（`docs/working/_reports/v8.20.0-release-note.md`）は `CHANGELOG.md` からの**機械抽出物**であり、独自の文面を持たない（二重管理を作らない）
- `docs/ai/versioning-stability-policy.md` は**書き換えていない**

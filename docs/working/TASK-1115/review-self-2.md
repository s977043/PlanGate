# 簡易 C-1 再セルフレビュー — TASK-1115 (#1115) / V-3 REJECT 反映後

> 初版 `review-self.md` は**書き換えず残す**（V-3 で REJECT された判断の記録として
> 保全）。本書は再設計後の簡易 C-1。

## 再レビュー対象

V-3 指摘（critical 1 / major 3 / minor 4 / info 2）を受けた
`plan.md` の判定設計・残存クラス、`test-cases.md`、実装、TC / 変異。

## Plan チェック（差分のみ）

| ID | 項目 | 判定 | 根拠 |
|----|------|------|------|
| C1-PLAN-01 | 受入基準の網羅性 | **PASS** | AC-7（保護ディレクトリ集合）/ AC-8（brace）/ AC-9（性能）を追加し TC・evidence に紐付け |
| C1-PLAN-02 | Unknowns の処理 | **PASS** | 残存クラス表を**実測 rc 付き**に全面改訂（V-3 R-009） |
| C1-PLAN-03 | スコープ制御 | **PASS** | `_has_write_intent` の既存ギャップ（V-3 R-010）と読み方向判定を follow-up として分離 |
| C1-PLAN-04 | テスト戦略 | **PASS** | 変異 10 種（うち 2 種は誤検出方向 = 負側でしか殺せない）。等価変異を正直に記録 |
| C1-PLAN-07 | 動作検証の自動化 | **PASS** | 3 版（pre / v1 / v2）× 両方向の実測表と性能ベンチを evidence 化 |

## 追加観点（V-3 指摘に対する自己検証）

| 観点 | 判定 | 根拠 |
|------|------|------|
| **軸の統一**（設計上の要求） | **PASS** | 「ディレクトリ条件 × basename 条件」の組に統一。除外は*形*ではなく*幅* |
| R-001 の封鎖 | **PASS** | 4 ケース `pre=0 / v1=0 → v2=2` |
| R-002 の封鎖 | **PASS** | 2 ケース `pre=0 / v1=0 → v2=2`。残存クラス化ではなく封鎖を選択 |
| R-003 の解消 | **PASS（9/11）+ 棄却（2/11）** | 棄却 2 件は既存リテラル挙動と整合することを実測で提示 |
| R-004 の解消 | **PASS** | fork ゼロ + 走査の遅延化。800 語 `14684ms → 116ms`（PRE 126ms = 劣化なし） |
| **両方向の実測** | **PASS** | `evidence/v3-both-directions.txt` に塞ぐべきもの / 誤検出であってはならないものを 1 表で提示 |
| 空振り変異の是正 | **PASS** | R-005 / R-007 に TC を追加し M-12 / M-13 で kill。R-008 は非等価性を TC-15 + M-14 で固定 |
| 負側 TC が本番経路を通るか | **PASS** | 全 TC が `t25_guard`（stdin に PreToolUse JSON）経由 |
| 絶対件数の assert | **PASS** | 新規 TC / 変異とも件数を等値比較しない |
| fail-closed を緩めていないか | **PASS** | 既存の判定不能 → block 経路は無改変 |

## 判定

**PASS**（FAIL 0 / WARN 0）

### 残る minor（plan / handoff に記載済み）

- **R-201 (minor)**: 読み方向（source 位置の引数）を block 対象から外す方向判定が
  無い。**既存設計**であり本 PBI で新規に生じたものではないが、V-3 R-003 #5/#11 の
  棄却根拠がここに依存しているため follow-up 候補として明示する。
- **R-202 (minor)**: ディレクトリ名と basename の**両方**を glob で崩す形は
  一部通る。残存クラス表に記載。

## C-3 について

Mode = `high-risk` のため `lite_eligible=false`、**autonomous APPROVE 不可**。
本ワーカーは `c3.json` を**発行しない**。
